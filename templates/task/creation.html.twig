{% extends 'base.html.twig' %}

{% block title %}
	{% if edit is defined %}
		Editar tarea
	{% else %}
		Crear tarea
	{% endif %}
{% endblock %}

{% block body %}

	<div class="example-wrapper">
		<h1>
			{% if edit is defined %}
				Editar tarea
			{% else %}
				Crear tarea
			{% endif %}
		</h1>
		{{ form_start(form) }}
		{{ form_widget(form) }}
		{{ form_end(form) }}
	</div>
{% endblock %}
